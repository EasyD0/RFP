# 列号单位不一致问题（暂缓解决）

## 现象

代码中把 clangd / Python 字符串索引得到的"列号"直接传给 libclang 时，
如果引用 token 所在行、token 之前存在非 ASCII 字符（如中文注释、中文标识符），
游标会定位到错误位置，导致取错节点。

## 根因

三套列号单位互不相同：

| 来源 | 单位 | 起始 |
| --- | --- | --- |
| clangd（LSP `position.character`） | UTF-16 code unit | 0 |
| libclang（`SourceLocation.column`） | UTF-8 字节 | 1 |
| Python `str.index()` / `re.Match.start()` | Unicode 码点 | 0 |

对纯 ASCII 字符，三者恰好一致（1 字符 = 1 字节 = 1 code unit），所以平时
测不出来；一旦同一行出现中文，三套数字全部错开。Tab 在三套里都按 1 计，
不受影响。

## 影响位置

1. `checker.py` Checker_36S `_is_return_value_used` 的调用处：
   `ref_loc["start"].get("character", 0)`（clangd UTF-16 列）直接传给
   `get_cursor_at_line`（libclang 字节列）。
2. `checker.py` Checker_47S 数组越界检查：
   `access_match.start() + 1`（Python 码点列）传给 `get_cursor_at_line`。

## 实测复现

对代码行 `int f(void) { /*中文*/ return arr[2]; }`：

- Python 码点列 34 传给 libclang → 取到 `arr` 的 `DECL_REF_EXPR`（错误节点）
- UTF-8 字节列 38 → 正确取到下标处的 `INTEGER_LITERAL`

错误方向多数是"取到外层/相邻节点后保守跳过"，但也可能进入错误分支，
存在误判风险，解决前不应依赖中文行。

## 为什么可以暂缓

当前扫描目标的引用/访问行，token 之前基本是 ASCII（缩进、`=`、`(` 等），
踩中概率低；且多数误定位是保守跳过而不是误判。

## 将来修复方案

1. 传给 libclang 前把字符偏移换算为字节偏移：
   `len(line_text[:char_offset].encode("utf-8")) + 1`。
2. clangd 初始化时协商 `offsetEncoding: "utf-8"`（若目标 clangd 版本支持）。
3. 在 `get_cursor_at_line` 内部统一转换，对外仍接收"逻辑列"。

注意：`Checker_47S` 中按 cursor extent 从源码取字面量文本时，也应使用字节
区间切片（`line.encode("utf-8")[start:end].decode("utf-8")`），不要用
Python 字符串下标直接切。
