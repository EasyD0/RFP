# 关于5个规则的识别误报
这项工作的立足点:
识别误报必须没有虚警 (误报), 即 $\mathbb{P}\{ p  实际上是误报| p被算法识别为误报\} = 1$
要做到如上保证, 必须形式上对算法做证明
## 69D-变量未赋值就使用
###### 已在声明处显式初始化
**核心逻辑:** 在声明/定义处已经显式初始化, 视为误报
仍然需要增强防护
**证明:** C 语言的初始化语句形式固定, 形式类型有限, 通过遍历所有初始化风格, 模式匹配判断是否为初始化, 可确保该子算法的正确性

###### 全局变量自动零初始化
**核心逻辑:** 若发现在全局作用域声明/定义, 则视为误报
**证明:** C 语言的全局变量判定是可计算问题, 从而可确保该子算法的正确性

###### 静态变量自动零初始化
**核心逻辑:** 若判断为静态变量, 则视为误报
**证明:** 声明为静态存储期的形式判断是可计算问题
###### 中途发现初始化语句
**核心逻辑:** *暂未实现*, 涉及到数据流分支追踪, 非常困难
不考虑 goto 语句, 考虑 C 语言的拓扑结构, 能否 证明/证伪 不存在一种通用算法可以在有效时间内, 追踪一个变量的所有可能路径, 以及路径的初始化语句集合?

退行方案:
```ad-faq
能否只看同一层级的数据流来判断误报?
证明: 这同一层级的初始化集是真实初始化集的子集

对于线性编程而言, 不考虑goto语句:
给定对变量$x$的引用语句语句$sta$
设集合$S_0$是当前函数块作用域中, 从头执行到语句$sta$处, 且和$sta$处同一语法树层级的对变量$x$执行初始化语句的集合
设集合$S$是当前函数块作用域中, 从头执行到语句$sta$处, 对变量$x$执行初始化语句的集合, 每次执行时的$S$由于经过不同的分支, 路径可能不同, 集合$S$可能不同

能否证明$S_0\subset S$

如果是函数式编程呢?
```

```cpp
void g(int);
void f(){
	int i;
	
	if (/* ?? */) {
		i = 1;
		return;
	}
	
	i = 1;
	g(i);
}
```

## 57S-无作用的语句
###### 未用的参数
**核心逻辑:** 检查语句是否为 `UNUSED_PARAMETER(...)` 宏调用, 若是则视为误报
**证明:** 宏调用形式固定 (`UNUSED_PARAMETER(x)`), 通过正则模式匹配可确保正确性

###### 日志打印
**核心逻辑:** 提取语句首个函数调用名, 若含 `log` 或 `print` 子串则视为误报
**证明:** 该子算法为启发式近似方法, **无法保证无虚警**. 反例: 用户自定义函数名含 `log`/`print` 但实际有副作用 (如 `log_and_commit()`). 需增强为基于函数签名/返回类型判断, 或维护已知日志函数白名单

###### 置为void
**核心逻辑:** 检查语句是否为 `(void) expr` 形式, 若是则视为误报
**证明:** `(void)` 强制丢弃表达式的形式固定, 正则 `\( *void *\)` 模式匹配可确保正确性

###### 声明语句
**核心逻辑:** 通过 libclang 获取语句的 cursor, 若 `cursor.kind == VAR_DECL` 则视为误报
**证明:** 基于 libclang 的语法分析, cursor 的 kind 属性由 C 语法解析器确定, 语义可靠. 前提是 libclang 本身正确解析了 C 代码

###### 调用函数
**核心逻辑:** 通过 cursor 判断语句是否为函数调用或函数指针调用:
- `DECL_REF_EXPR` 且定义是 `FUNCTION_DECL` → 函数调用
- `DECL_REF_EXPR` 且类型含 `(*)` → 函数指针调用
- `PAREN_EXPR` 且类型含 `(*)` → 括号包裹的函数指针调用
**证明:** 基于 libclang 语义分析, 已覆盖常见情况. 但 `DECL_REF_EXPR` 分支存在未覆盖情况 (`raise NotImplementedError`), 完备性待补全

## 1X-在整个系统中声明的类型不一致
###### 代码行没有两处
**核心逻辑:** 若问题代码行不恰好包含两处声明, 视为误报
**证明:** 简单的计数判断 (`len(code_line) != 2`), 可确保正确性

###### 两个声明类型和链接,存储属性均一致
**核心逻辑:** 获取两处声明的 cursor, 依次比较:
1. 存储类别 (storage_class): 若一方为 `STATIC` 或 `REGISTER` 而另一方不同 → 非误报
2. 链接属性 (linkage): 若一方为 `INTERNAL` 而另一方不同 → 非误报
3. 类型 (type.get_canonical()): 若类型拼写不同 → 非误报
若三者均一致, 则视为误报

基于 libclang 的语义分析, 存储类型/链接属性/类型均可由 cursor 精确获取. 该子算法判断"两处声明是否一致", 一致则误报, 不一致则保留为真实问题. 前提是 libclang 正确解析了两处声明

## 47-数组越界
### 数组越界检查的不可判定性

**结论:** 求解任意 C 程序是否发生数组越界, 是一个不可判定问题.

**证明 (归约到停机问题):**

定义问题 ABP (Array Bounds Problem): 给定任意 C 程序 $P$, 判断 $P$ 在某次执行中是否会发生数组越界.

假设存在判定器 $A$ 解决 ABP. 给定图灵机 $M$ 和输入 $w$, 构造 C 程序 $P_{M,w}$:

```cpp
int main() {
    int arr[1];
    simulate(M, w);  // C 是图灵完备的, 可用动态数组模拟纸带逐步执行
    arr[1];           // 越界: 仅当 M(w) 停机时才会执行到此
    return 0;
}
```

其中 `simulate(M, w)` 逐步模拟 $M$ 在 $w$ 上的执行; $M$ 停机时返回, 不停机则永不返回.

$$ M(w) \text{ 停机} \iff P_{M,w} \text{ 执行到 } arr[1] \iff P_{M,w} \text{ 发生数组越界} $$

若 $A$ 存在, 运行 $A(P_{M,w})$ 即可判定 $M(w)$ 是否停机, 矛盾于停机问题的不可判定性 (图灵, 1936). 故 $A$ 不存在, ABP 不可判定. $\blacksquare$

> **Rice 定理视角:** "程序是否发生数组越界" 是程序计算的非平凡语义性质, Rice 定理直接给出不可判定结论.

**受限子集的可判定性:** 上述不可判定性针对任意 C 程序. 对下标为编译期常量的受限情况 (字面量、枚举常量), 数组越界检查退化为可判定问题, 见下文子算法.

###### 数组长度检查确保不越界
**核心逻辑:**
1. 从规则名中解析数组名和下标
2. 获取数组 cursor, 求其长度 `arr_size`
3. 获取下标 cursor, 判断下标类型:
   - 字面量下标: 若 `0 <= int(spelling) < arr_size` 则视为误报 (有长度检查)
   - 枚举常量下标: 若 `0 <= enum_value < arr_size` 则视为误报
   - 普通变量下标: *暂未实现*, 保守不判误报 (不抛异常)
**证明:**
- 字面量/枚举下标: 编译期常量, 基于 libclang 语义分析可直接比较, 可确保正确性
- 变量下标: 如上文所证, 一般情况下不可判定. 退行方案: 仅检查同一语法树层级是否存在对下标变量的约束语句 (如 `if (i < arr_size)`), 作保守近似 (sound over-approximation)

## 36S-函数没有返回语句
###### 所有引用处都没有使用返回值
**核心逻辑:**
1. 通过 clangd 定位函数的所有引用
2. 逐个检查引用处代码:
   - 若引用处含 `=` → 存在赋值, 非误报
   - 若引用处以 `func_name(` 或 `(void) func_name(` 开头 → 不使用返回值, 跳过
   - 否则 → 使用了返回值, 非误报
3. 若所有引用处都不使用返回值, 则视为误报

**证明:**
- 引用查找: 基于 clangd LSP 协议, 可靠
- "是否使用返回值"判断: 通过正则匹配 `func_name(` 开头 (允许 `(void)` 前缀), 覆盖直接调用和显式丢弃返回值两种形式. 函数名经 `re.escape` 转义, 末尾要求 `(` 确保是函数调用且避免同前缀标识符误匹配
- **局限性:** 仅检查了直接调用形式, 未覆盖 `ret = func()` 后 `ret` 未使用的情况; 也未覆盖函数指针赋值 (`fp = func`) 后通过指针调用的形式


## 附录 Rice 定理
###### 定义: 图灵机
图灵机是一个七元组所表示的对象

$$M = (Q, \Sigma, \Gamma, \delta, q_0, q_{accept}, q_{reject})$$
其中
- $Q$: 有限的状态集合。
- $\Sigma$:输入字母表 (不含空白符 $\sqcup$)。
- $\Gamma$: 纸带字母表, 满足 $\Sigma \subseteq \Gamma$, $\sqcup \in \Gamma$。
- $\delta: Q \times \Gamma \to Q \times \Gamma \times \{L, R\}$: 转移函数, 即"当前状态 + 当前读到的符号 → 下一状态 + 写入的符号 + 移动方向"的规则表。这是程序的具体逻辑本身。
- $q_0$: 初始状态。
- $q_{accept}, q_{reject}$: 接受 / 拒绝状态, $q_{accept} \ne q_{reject}$。

给定输入 $x \in \Sigma^*$, 机器从 $q_0$ 出发按 $\delta$ 逐步运行, 直到进入 $q_{accept}$ 或 $q_{reject}$ (称为**停机**, halt), 或者永远不会进入这两个状态之一 (称为**不停机 / 发散**)。
$M$ 本身是一个**静态的、有限长度的语法对象**——可以编码成一个字符串 $\langle M \rangle$, 与任何具体输入无关。这就是"程序代码"。

###### 定义: 部分函数和全函数
全函数对于定义域上的所有值, 都给出输出. 部分函数对于某些输入无法输出 (发散)
程序对应的函数是一个 $\Sigma^* \rightharpoonup \Sigma^*$ 的部分函数。其中 $\Sigma^* = \bigcup_{n=0}^{\infty} \Sigma^n$
###### 定义: 程序计算的函数
程序当输入为 $x$ 时运行的结果可表述为:
$$
M(x) = \begin{cases}
y & M \text{ 从 } x \text{ 出发,经有限步进入 } q_{accept},\text{ 此时纸带内容为 } y\\
\uparrow & M \text{ 从 } x \text{ 出发永不停机}
\end{cases}
$$
图灵机 $M$ **计算的函数** 是一个*部分函数*, 它为:
$$f_M : \Sigma^* \rightharpoonup \Sigma^*, \qquad f_M(x) = \begin{cases} y & \text{若 } M(x) = y \\ \text{未定义} & \text{若 } M(x) = \uparrow \end{cases}$$

即一个映射程序 $M$ 所有输入输出情况的函数. 
- 两个程序**计算相同函数** 的含义为: $f_{M_1} = f_{M_2} \iff \forall x,\ \big(f_{M_1}(x)=f_{M_2}(x)\big) \lor \big(\text{两者在 } x \text{ 上都未定义}\big)$
- 不同的 $M$ 可以对应相同的 $f_M$
###### 定义: 图灵机的性质
设 $\mathcal{L}$ 为全体图灵机编码组成的集合. 一个**性质** $P$ 就是 $\mathcal{L}$ 的一个子集 $P \subseteq \mathcal{L}$.
称"$M$ 具有性质 $P$", 记作 $\langle M \rangle \in P$.
相应地可以定义计算的函数的性质。设全体图灵机计算的函数集合为 $\mathcal{\hat{L}}$, 性质 $\hat{P}$ 是它的一个子集
###### 定义: 图灵机的语义性质
$P \subseteq \mathcal{L}$ 称为*语义性质*, 当且仅当
$$\forall M_1, M_2 \in \mathcal{L}: \quad f_{M_1} = f_{M_2} \implies \big(\langle M_1\rangle \in P \iff \langle M_2\rangle \in P\big)$$
即: $P$ 是否成立只取决于 $f_M$ (输入输出行为), 与 $M$ 具体细节无关。
等价地说, 存在定义在部分函数集合上的性质 $\hat P$, 使得 $\langle M\rangle \in P \iff f_M \in \hat P$。
###### 定义: 非平凡性质
$P \subseteq \mathcal{L}$ 称为*非平凡的*, 当且仅当
$$P \ne \emptyset \quad \text{且} \quad P \ne \mathcal{L}$$

即: 存在至少一台机器具有 $P$, 也存在至少一台机器不具有 $P$。

###### 定义: 可判定的
性质 $P \subseteq \mathcal{L}$ 称为*可判定的*, 当且仅当存在一个图灵机 $R$ (判定器), 使得对任意 $\langle M \rangle \in \mathcal{L}$:
$$
R(\langle M \rangle) = \begin{cases}
\text{接受(输出"是")} & \text{若 } \langle M \rangle \in P \\
\text{拒绝(输出"否")} & \text{若 } \langle M \rangle \notin P
\end{cases}
$$

并且 $R$ 对任意输入都保证停机 (即 $R$ 本身对应的函数是全函数)。
也就是说, 不仅要给出正确答案, 还必须保证在有限时间内一定给出答案, 不能自己也陷入死循环。

###### 定理: 图灵停机定理 (Halting Problem 的不可判定性)
以下性质
$$HALT = \{\langle M, w \rangle : M \text{ 是图灵机},\ M \text{ 在输入 } w \text{ 上停机}\}$$
是不可判定的。即不存在图灵机 $R$, 对任意 $\langle M, w\rangle$ 都能在有限步内正确判定 $M$ 在 $w$ 上是否停机。
**证明(对角化法, Turing 1936)**: 
反证。假设存在判定器 $H$, 对任意 $\langle M, w\rangle$ 判定 $M(w)$ 是否停机。构造一台新机器 $D$:
$$D(\langle M \rangle) = \begin{cases} \text{死循环(不停机)} & \text{若 } H(\langle M, M\rangle) = \text{"停机"} \\ \text{停机(接受)} & \text{若 } H(\langle M, M\rangle) = \text{"不停机"} \end{cases}$$
即 $D$ 把 $M$ 的编码同时当作程序和输入喂给自身。考虑 $D(\langle D \rangle)$:
- 若 $D$ 在 $\langle D\rangle$ 上停机 $\Rightarrow$ 按 $D$ 的定义, 这发生的条件是 $H(\langle D,D\rangle)=$"不停机" $\Rightarrow$ 矛盾。
- 若 $D$ 在 $\langle D\rangle$ 上不停机 $\Rightarrow$ 按 $D$ 的定义, 这发生的条件是 $H(\langle D,D\rangle)=$"停机" $\Rightarrow$ 矛盾。
两种情况都自相矛盾, 所以 $H$ 不存在。
###### 定理: Rice 定理
设 $P \subseteq \mathcal{L}$ 是**语义性质**且**非平凡**, 则 $P$ **不可判定**。
**证明 (归约到 HALT)**: 
不失一般性设处处发散的函数 $\emptyset \notin \hat P$ (否则对补集 $\bar P$ 证明, 可判定性对补运算封闭)。由非平凡性, 存在 $M_Y$ 使 $f_{M_Y} \in \hat P$。
假设存在判定器 $R$ 判定 $P$。给定任意 $HALT$ 实例 $\langle M, w\rangle$, 构造机器 $M'$:

> $M'(x)$: 先模拟 $M$ 在 $w$ 上运行; 若这一步停机, 则接着模拟 $M_Y(x)$ 并输出其结果; 若不停机, $M'$ 也不停机。

- 若 $M$ 不在 $w$ 上停机 $\Rightarrow f_{M'} = \emptyset \notin \hat P \Rightarrow \langle M'\rangle \notin P$
- 若 $M$ 在 $w$ 上停机 $\Rightarrow f_{M'} = f_{M_Y} \in \hat P \Rightarrow \langle M'\rangle \in P$

$M'$ 可由 $\langle M, w\rangle$ 有效构造。于是 $M$ 在 $w$ 上停机 $\iff R(\langle M'\rangle)=$"是", 这就给出了 $HALT$ 的判定算法, 与图灵停机定理矛盾。故 $R$ 不存在。∎


###### 为什么"数组越界"是语义性质
设 $P_{\text{OOB}} = \{\langle M \rangle : M \text{ 在其运行过程中会发生数组越界访问}\}$ (这里把"是否发生越界"整理成 $M$ 输入输出/运行行为的一部分, 即视为 $f_M$ 所刻画信息的一部分, 例如约定越界时输出特殊标记或崩溃状态)。
按语义性质的定义, 要验证: $f_{M_1} = f_{M_2} \implies (\langle M_1\rangle \in P_{\text{OOB}} \iff \langle M_2\rangle \in P_{\text{OOB}})$。
**成立的原因**: "是否会越界"这件事, 完全由程序在给定输入下的**实际运行轨迹和读写行为**决定——即完全由 $f_M$ (输入到输出/行为的对应关系) 决定, 不依赖 $M$ 的代码是怎么写的。
- 如果 $M_1$ 和 $M_2$ 是两段代码完全不同 (变量名不同、用循环还是递归、语句顺序不同), 但对每个输入 $x$ 都产生**完全相同**的行为 (包括是否越界), 那么 $f_{M_1}=f_{M_2}$, 此时它们要么同时越界要么同时不越界——判定结果只取决于函数本身, 与代码写法无关。
- 换个角度看: 你无法找到"越界与否"依赖于源代码文本本身而非依赖于实际执行行为的例子——两份实现了同一输入输出行为 (含是否越界) 的代码, 天然就在这条性质上取值一致。这正符合语义性质的定义。

同时它显然**非平凡**:存在会越界的程序 (如 `a[n+1]` 访问越界), 也存在不会越界的程序 (如安全的边界检查代码)。
因此 $P_{\text{OOB}}$ 是非平凡的语义性质, 根据 Rice 定理,**不存在通用算法能对所有程序精确判定其是否存在数组越界**。这正是为什么静态分析工具 (包括你在 Testbed 中做的 index range checker) 只能通过 interval domain 抽象解释等方法做**近似**判断——必须在"漏报" (unsound) 和"误报" (incomplete) 之间取舍, 无法两者兼得。

