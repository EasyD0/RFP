# 误报识别项目说明 (ReduceFalsePositives)

本文档描述项目的目标、模块结构与各规则的子算法实现。

算法依赖分为四类，各规则表中用下述标注：

| 标注 | 含义 |
| --- | --- |
| `正则` | Python 正则 / 文本匹配，面向形式固定的代码写法 |
| `libclang` | clang.cindex（libclang）AST 语法与语义分析 |
| `clangd` | clangd（LSP）跨翻译单元符号引用查询 |
| `无` | 纯逻辑 / 报告元数据判断，无外部工具 |

---

## 1. 项目背景

### 1.1 目标

静态分析工具会对源码报告大量"问题"，每条问题包含文件名、函数名、代码行、规则名称、违反码、问题描述等信息。其中相当一部分是误报——代码实际没有违反规则，但分析器报了出来。本项目针对报告中的每条问题自动判定"是否为误报"，从而减少人工复核的成本。

### 1.2 立足点：无虚警保证

识别误报必须**没有虚警**，即

$$\mathbb{P}\{ p \text{ 实际上是误报} \mid p \text{ 被算法识别为误报} \} = 1$$

换句话说：算法只要把某个问题标记为误报，就必须保证它确实是误报；不确定时**宁可保守不判**（保留为真实问题），也不冒险把真实缺陷漏掉。

要做到这一点，每个子算法都必须有形式化证明或可验证的依据：

- 形式固定的代码模式（可用正则精确匹配）；
- 可判定的语法/语义属性（全局作用域、静态存储期、编译期常量下标等）；
- 可靠的 AST 语义信息（cursor 种类、存储类别、链接属性、canonical 类型、数组长度、枚举值、宏定义值）。

启发式规则（如按函数名猜测日志打印）无法满足无虚警要求，只能作为过渡方案，或需要增强为基于函数签名/白名单的判断。

### 1.3 规则范围

目前覆盖 5 条规则，另有一个设计上附加到所有规则的通用检查：

| 违反码 | 规则 | 问题形态 |
| --- | --- | --- |
| 69D | 变量未赋值就使用 | 变量在声明后、赋值前被读取 |
| 57S | 无作用的语句 | 语句执行结果被丢弃，形同无效 |
| 1X | 在整个系统中声明的类型不一致 | 同一符号的多次声明在类型/链接/存储属性上不一致 |
| 47S | 数组越界 | 下标可能超出数组边界 |
| 36S | 函数没有返回语句 | 声明有返回值的函数缺少 return |
| （通用） | 未参与编译文件 | 报告文件不参与编译 → 误报 |

### 1.4 技术选型

| 技术 | 角色 | 特点 |
| --- | --- | --- |
| 正则/文本匹配 | 识别形式固定的写法：宏调用、`(void)` 表达式、`&var`、`#pragma inline_asm` 等 | 成本最低；形式固定时正确性可保证，但无法做语义判断 |
| libclang（clang.cindex） | AST 语法/语义分析：cursor 种类、存储类别、链接属性、canonical 类型、数组长度、枚举值、宏定义值、语句块/父节点关系 | 语义可靠（前提是 clang 正确解析）；需要配置动态库路径与编译参数 |
| clangd（LSP） | 跨翻译单元的符号引用查询（`textDocument/references`），用于"所有引用处"类判断 | 引用信息最完整，但启动 + 后台索引成本高 |
| 纯逻辑 | 报告字段计数/元数据判断（代码行数量、全局作用域函数名等） | 无外部依赖 |

### 1.5 可判定性与保守策略

有些目标性质对任意 C 程序不可判定，例如"是否会发生数组越界"（归约到停机问题，见附录 4.2）。因此项目采取**保守近似**策略：

- 只对能证明（固定形式 / 可计算属性 / 可靠 AST 信息）的情况判误报；
- 证明不了时保留问题不判，不抛异常；
- 复杂场景先实现"退行版本"（如只检查同一语法层级），保持"判了必对"，后续再增强覆盖。

---

## 2. 模块设计

### 2.1 目录结构

```
ReduceFalsePositives/
├── 说明.md                     # 本文档
├── 列号单位问题.md              # clangd/libclang/Python 列号单位不一致的已知问题
├── data_structure.py           # 问题数据模型（Problem / CodeLine / CodePos / RuleName）
├── test.py                     # 69D 子算法最小测试
├── MyPyLib/                    # 通用库
│   ├── Common.py               # 计时装饰器（占位）
│   ├── LogSet.py               # 日志封装
│   └── Preprocessor.py         # 编译上下文预处理（编译参数 / 参与编译文件）
├── clang_tool/                 # libclang 封装
│   ├── config.py               # libclang 动态库路径与 C 编译参数
│   ├── parse.py                # 翻译单元解析、按行列取游标
│   ├── ast_utils.py            # AST 结构查询（父节点映射 / 祖先 / 块 / 同层 / 约束）
│   ├── value.py                # 整型字面量 / 宏定义值提取
│   └── demo.py                 # 使用示例
├── clangd_tool.py              # clangd LSP 客户端（长驻会话 + 引用查询）
├── clangd_service.py           # 持久化 clangd 服务（另一版封装，备用）
├── checker/                    # 检查框架
│   ├── base.py                 # Checker 基类 / 装饰器 / 注册表 / 通用检查
│   ├── context.py              # CodeContext 编译上下文 + compile_commands.json
│   ├── runner.py               # 串行 / 多进程调度
│   └── rules/                  # 各规则实现（导入即注册）
│       ├── rule_69d.py         # 69D 变量未赋值就使用
│       ├── rule_57s.py         # 57S 无作用的语句
│       ├── rule_1x.py          # 1X 声明的类型不一致
│       ├── rule_47s.py         # 47S 数组越界
│       └── rule_36s.py         # 36S 函数没有返回语句
└── test_macro*/ test/          # clangd 引用测试与样例工程
```

### 2.2 模块职责

| 模块 | 职责 | 关键依赖 |
| --- | --- | --- |
| `data_structure.py` | 从报告文本解析 `Problem`（含代码行、规则名、违反码），维护误报状态与排序 | 无 |
| `MyPyLib/Preprocessor.py` | 提供编译参数（`get_args`）与参与编译文件集合（`getUsedFiles`）；当前为接口占位实现 | 编译工程数据（.resp） |
| `MyPyLib/LogSet.py` | 统一日志格式 | 无 |
| `clang_tool/` | libclang 的一切封装：解析 TU、按行列取 cursor、父节点映射与祖先查询、函数/块/同层节点、字面量与宏值提取 | libclang |
| `clangd_tool.py` | 长驻 clangd 会话（按 project/compile-dir 缓存）、后台索引等待、`textDocument/references` | clangd + compile_commands.json |
| `clangd_service.py` | 另一套持久化 clangd 服务（单进程内复用、didOpen 缓存） | clangd |
| `checker/base.py` | `Checker` 基类与装饰器（注册/标记/通用），`CheckerDict` 注册表，`Checker_isUsed` 通用检查 | — |
| `checker/context.py` | `CodeContext`：初始化 Preprocessor、生成 compile_commands.json、提供编译参数与文件归属；整体可 pickle 供多进程复用 | Preprocessor + 文件系统 |
| `checker/runner.py` | `total_check` 串行 / `total_check_parallel` 多进程；worker 复用反序列化后的 CodeContext，结束后清理孤儿 clangd 进程 | multiprocessing |
| `checker/rules/*.py` | 5 条规则的子算法实现，导入即注册 | 正则 / libclang / clangd |

### 2.3 数据流与执行机制

1. 从静态分析报告解析出 `Problem` 列表（每条问题含 code_line、rule_name、rule_code 等）。
2. 构建 `CodeContext`：Preprocessor 收集参与编译的文件集合与每文件的编译参数，并生成 compile_commands.json（供 clangd 使用）。
3. runner 按 `rule_code` 从 `CheckerDict` 聚合检查器（每条规则还附加 `Checker_isUsed` 通用检查）。
4. 对每个 `Problem` 依次执行各检查器的 `do()`：检查器内子算法按方法名排序顺序执行，一旦判为误报立即短路（后续子算法不再运行）。
5. 命中误报时，子算法的 tag（如 `<已在声明处显式初始化>`）会追加到问题描述，便于追溯判定来源。
6. 统计并输出误报率；多进程场景下 worker 通过 initializer 反序列化共享的 CodeContext（不重复初始化），Pool 退出后调用 `kill_all_clangd_processes()` 兜底清理 clangd 进程。

### 2.4 关键机制与注意事项

- **注册与标记**：`register_checker` 注入 rule_code 并收集带标记的方法；`tag_padding` 标记子算法并负责追加 tag；`common_method` 标记通用子算法；`un_used` 标记暂不启用的方法。
- **通用检查状态**：`Checker_isUsed`（"未参与编译文件"）设计上附加到所有规则，但当前 runner 以默认参数调用其 `do()` 时方法会提前返回，检查实际不生效（处于待启用状态）。
- **已知问题**：列号单位在 clangd（UTF-16 code unit，0-based）、libclang（UTF-8 字节，1-based）、Python（Unicode 码点，0-based）三处不一致，影响 36S/47S 在含非 ASCII 字符行上的定位，详见 [列号单位问题.md](列号单位问题.md)。

---

## 3. 各规则子算法一览

### 3.1 69D 变量未赋值就使用

针对"声明后未赋值就使用"的报告，识别各种实际已初始化的误报情形。

| 子算法（tag） | 核心逻辑 | 技术依赖 | 正确性 / 状态 |
| --- | --- | --- | --- |
| 代码行没有两处（通用） | 问题代码行不足两处（`len(code_line) != 2`）→ 误报 | 无（纯逻辑） | 固定形式，可保证 |
| 已在声明处显式初始化（通用） | 声明行含 `=` 且不含 `==` → 已显式初始化 | 文本匹配 | C 初始化形式有限，遍历所有初始化风格可保证 |
| 全局变量自动零初始化（通用） | 函数名为空或为 "Whole program"（全局作用域）→ 自动零初始化 | 无（报告元数据） | 全局作用域判定可计算 |
| 静态变量自动零初始化 | 声明行匹配 `\sstatic\s`；否则用 libclang 取 cursor，判断 `storage_class == STATIC` | 正则 + libclang | 兼容 `STATIC` 等宏声明；静态存储期判定可计算 |
| 中途发现初始化语句 | 在使用处所在函数的最内层块中，取起始位置不晚于使用处的**同一层级**语句，文本匹配 `var = ...`、`&var`、`&var.member` → 视为已初始化 | libclang + 正则 | 退行实现：只查同一层级，未找到证据则保守不判；全路径分支追踪难度大（见附录 4.4） |
| 此处没有直接使用变量值，而是使用其地址（通用） | 使用行匹配 `&var` → 实为传地址供函数赋值 | 正则 | 固定形式 |
| 未参与编译文件（附加通用） | 报告文件不在编译文件集合 → 误报 | 无（Preprocessor） | 见 2.4，当前调用路径下不生效 |

### 3.2 57S 无作用的语句

针对"语句无作用"的报告，识别宏占位、日志、显式丢弃、汇编、声明、函数调用等实际有含义或无需处理的语句。

| 子算法（tag） | 核心逻辑 | 技术依赖 | 正确性 / 状态 |
| --- | --- | --- | --- |
| 未用的参数（通用） | 语句匹配 `UNUSED_PARAMETER(` 宏调用 | 正则 | 宏形式固定，可保证 |
| 日志打印（通用） | 提取语句首个函数调用名，含 `log`/`print` 子串 | 文本匹配（启发式） | 无法保证无虚警（如 `log_and_commit()`）；需增强为函数签名/白名单判断 |
| 置为void（通用） | 匹配 `(void) expr` 强制丢弃形式 | 正则 | 形式固定，可保证 |
| 识别为汇编语言 | 回溯到含 `{` 的行，检查上方是否有 `#pragma inline_asm func` 或 `__asm` | 正则 + 源码读取 | 覆盖两种内联汇编写法 |
| 声明语句 | libclang cursor.kind == VAR_DECL | libclang | 语法解析可靠 |
| 调用函数 | DECL_REF_EXPR 且定义为 FUNCTION_DECL / 类型为函数指针；PAREN_EXPR 为函数指针；CALL_EXPR | libclang | 常见情况已覆盖；DECL_REF_EXPR 分支存在未覆盖情况，完备性待补全 |

### 3.3 1X 在整个系统中声明的类型不一致

针对"同一符号多处声明不一致"的报告，判断两处声明是否在语义上完全一致。

| 子算法（tag） | 核心逻辑 | 技术依赖 | 正确性 / 状态 |
| --- | --- | --- | --- |
| 代码行没有两处（通用） | 问题代码行不足两处 → 误报 | 无（纯逻辑） | 固定形式，可保证 |
| 两个声明的类型、链接、存储属性均一致 | 取两处声明 cursor，依次比较：storage_class（一方为 STATIC/REGISTER 且不同 → 非误报）、linkage（一方为 INTERNAL 且不同 → 非误报）、canonical type（`uint8_t` 与 `bool` 视为一致）；全部一致 → 误报 | libclang | 语义属性可由 cursor 精确获取；依赖 libclang 正确解析 |

> 注：曾有一个"两个声明文本一致"的文本比较子算法，已注释禁用，统一改为上述语义比较。

### 3.4 47S 数组越界

> 任意 C 程序是否发生数组越界是不可判定问题（归约到停机问题，见附录 4.2）。因此本规则只对**编译期可判定**的下标形式判误报，其余情况一律保守不判。

| 子算法（tag） | 核心逻辑 | 技术依赖 | 正确性 / 状态 |
| --- | --- | --- | --- |
| 发现充分的下标约束 | 从规则名解析数组名与下标，取数组长度 `arr_size`，再按下标形式分支 | libclang + 正则 | 总体保守：无法证明界内则不判 |
| ├ 字面量下标 | `INTEGER_LITERAL` 值满足 `0 ≤ idx < arr_size` → 误报 | libclang（值提取） | 编译期常量，可保证 |
| ├ 枚举常量下标 | 引用定义为 `ENUM_CONSTANT_DECL`，`enum_value` 界内 → 误报 | libclang | 编译期常量，可保证 |
| ├ 宏下标 | `MACRO_INSTANTIATION` → 解析宏定义（仅支持简单对象宏，如 `#define N (3)`）值界内 → 误报 | libclang（宏定义 token 解析） | 复杂替换列表不支持，保守不判 |
| └ 变量下标 | 定位最近约束语句（for/while/if），用 `get_constraint` 提取约束并判断是否可证 `0 ≤ idx < arr_size`；约束区域内有对下标的写操作则保守退出 | libclang | 可证明界内才判，无虚警（见下方说明） |
| 复杂/跨行/多种下标 | 下标为复合表达式、跨行或同一行存在多种下标 → 保守退出 | — | 不判误报，不抛异常 |

**变量下标约束分析说明：**

- 支持 for / while 循环条件中的比较（`i < 10`）、for 初始化 `i = C` 在
  递增/递减递进下推导出的边界（`i >= C` / `i <= C`）、if then 分支中的
  条件比较；访问位于 else 分支、循环条件/递进中时条件不成立，不取约束。
- 支持 `&&` 组合、反向书写（`0 <= i` / `5 > i`）、宏上界（`i < N`）、
  枚举与 const 整型常量；含 `||`、取反、函数调用等无法保守拆分的逻辑时跳过。
- 充分性：等式约束 `i == C` 且 `0 ≤ C < arr_size`，或上界（`i < U` / `i <= U`
  且 `U ≤ arr_size`）与下界（`i >= L` / `i > L` 且 `L ≥ 0`）同时成立；
  无符号下标天然满足下界。
- 保守防护：循环体 / if then 分支内对下标变量的赋值、复合赋值、自增自减会使
  约束在访问点失效，一律不判误报。
- 局限：只做单层约束，不组合外层约束语句；不追踪指针别名 / 函数调用对下标的
  修改；while 循环体内含自增语句时保守不判；命令行宏（`-DN=5`）边界不解析。

### 3.5 36S 函数没有返回语句

针对"声明有返回值但可能缺少 return"的报告，判断函数所有引用处是否都不使用返回值。

| 子算法（tag） | 核心逻辑 | 技术依赖 | 正确性 / 状态 |
| --- | --- | --- | --- |
| 所有引用处都没有使用返回值 | clangd 定位函数全部引用；逐处检查：含 `=` → 使用了返回值（非误报）；否则正则判断是否以 `func_name(` 或 `(void) func_name(` 开头，再用 libclang AST 验证引用所在 CallExpr 的父节点（CompoundStmt / `(void)` 强转 / 无大括号控制流体 → 返回值未使用）；全部未使用 → 误报 | clangd + 正则 + libclang | 引用定位可靠；正则与 AST 双重校验；局限：未覆盖 `ret = func()` 后 ret 未使用、函数指针赋值 `fp = func` 后经指针调用等 |

### 3.6 通用检查（附加到所有规则）

| 子算法（tag） | 核心逻辑 | 技术依赖 | 状态 |
| --- | --- | --- | --- |
| 未参与编译文件 | 报告文件不在 `all_used_files` 中 → 误报 | 无（Preprocessor 编译列表） | 设计上已附加到每条规则；当前调用路径不生效（见 2.4） |

---

## 4. 附录：理论背景

### 4.1 为什么需要"可判定"与"形式证明"

无虚警保证要求"判为误报 ⇒ 确实误报"。对于不可判定的语义性质，不存在能对所有程序给出正确判定的通用算法；此时只能采用保守近似（只判可证明为误报的情况），并在理论上明确每个子算法的可判定边界。本附录给出两条核心结论：数组越界检查的不可判定性（归约到停机问题），以及其背后的 Rice 定理；并记录 69D"中途发现初始化语句"的复杂度讨论。

### 4.2 数组越界检查的不可判定性

**结论：** 求解任意 C 程序是否发生数组越界，是一个不可判定问题。

**证明（归约到停机问题）：**

定义问题 ABP（Array Bounds Problem）：给定任意 C 程序 $P$，判断 $P$ 在某次执行中是否会发生数组越界。

假设存在判定器 $A$ 解决 ABP。给定图灵机 $M$ 和输入 $w$，构造 C 程序 $P_{M,w}$：

```c
int main() {
    int arr[1];
    simulate(M, w);  // C 是图灵完备的, 可用动态数组模拟纸带逐步执行
    arr[1];          // 越界: 仅当 M(w) 停机时才会执行到此
    return 0;
}
```

其中 `simulate(M, w)` 逐步模拟 $M$ 在 $w$ 上的执行；$M$ 停机时返回，不停机则永不返回。

$$ M(w) \text{ 停机} \iff P_{M,w} \text{ 执行到 } arr[1] \iff P_{M,w} \text{ 发生数组越界} $$

若 $A$ 存在，运行 $A(P_{M,w})$ 即可判定 $M(w)$ 是否停机，矛盾于停机问题的不可判定性（图灵，1936）。故 $A$ 不存在，ABP 不可判定。$\blacksquare$

> **Rice 定理视角：** "程序是否发生数组越界"是程序计算的非平凡语义性质，Rice 定理直接给出不可判定结论。

**受限子集的可判定性：** 上述不可判定性针对任意 C 程序。对下标为编译期常量的受限情况（字面量、枚举常量），数组越界检查退化为可判定问题，见 3.4 的子算法。

### 4.3 Rice 定理

###### 定义：图灵机

图灵机是一个七元组所表示的对象

$$M = (Q, \Sigma, \Gamma, \delta, q_0, q_{accept}, q_{reject})$$

其中
- $Q$：有限的状态集合；
- $\Sigma$：输入字母表（不含空白符 $\sqcup$）；
- $\Gamma$：纸带字母表，满足 $\Sigma \subseteq \Gamma$，$\sqcup \in \Gamma$；
- $\delta: Q \times \Gamma \to Q \times \Gamma \times \{L, R\}$：转移函数，即"当前状态 + 当前读到的符号 → 下一状态 + 写入的符号 + 移动方向"的规则表；
- $q_0$：初始状态；
- $q_{accept}, q_{reject}$：接受 / 拒绝状态，$q_{accept} \ne q_{reject}$。

给定输入 $x \in \Sigma^*$，机器从 $q_0$ 出发按 $\delta$ 逐步运行，直到进入 $q_{accept}$ 或 $q_{reject}$（称为**停机**，halt），或者永远不会进入这两个状态之一（称为**不停机 / 发散**）。

$M$ 本身是一个**静态的、有限长度的语法对象**——可以编码成一个字符串 $\langle M \rangle$，与任何具体输入无关。这就是"程序代码"。

###### 定义：部分函数和全函数

全函数对于定义域上的所有值都给出输出；部分函数对某些输入无法输出（发散）。程序对应的函数是一个 $\Sigma^* \rightharpoonup \Sigma^*$ 的部分函数，其中 $\Sigma^* = \bigcup_{n=0}^{\infty} \Sigma^n$。

###### 定义：程序计算的函数

程序当输入为 $x$ 时运行的结果可表述为：

$$
M(x) = \begin{cases}
y & M \text{ 从 } x \text{ 出发，经有限步进入 } q_{accept}，\text{ 此时纸带内容为 } y\\
\uparrow & M \text{ 从 } x \text{ 出发永不停机}
\end{cases}
$$

图灵机 $M$ **计算的函数**是一个*部分函数*：

$$f_M : \Sigma^* \rightharpoonup \Sigma^*, \qquad f_M(x) = \begin{cases} y & \text{若 } M(x) = y \\ \text{未定义} & \text{若 } M(x) = \uparrow \end{cases}$$

即映射程序 $M$ 所有输入输出情况的函数。
- 两个程序**计算相同函数**：$f_{M_1} = f_{M_2} \iff \forall x,\ \big(f_{M_1}(x)=f_{M_2}(x)\big) \lor \big(\text{两者在 } x \text{ 上都未定义}\big)$；
- 不同的 $M$ 可以对应相同的 $f_M$。

###### 定义：图灵机的性质

设 $\mathcal{L}$ 为全体图灵机编码组成的集合。一个**性质** $P$ 就是 $\mathcal{L}$ 的一个子集 $P \subseteq \mathcal{L}$。称"$M$ 具有性质 $P$"，记作 $\langle M \rangle \in P$。相应地可以定义计算的函数的性质：设全体图灵机计算的函数集合为 $\mathcal{\hat{L}}$，性质 $\hat{P}$ 是它的一个子集。

###### 定义：图灵机的语义性质

$P \subseteq \mathcal{L}$ 称为*语义性质*，当且仅当

$$\forall M_1, M_2 \in \mathcal{L}: \quad f_{M_1} = f_{M_2} \implies \big(\langle M_1\rangle \in P \iff \langle M_2\rangle \in P\big)$$

即：$P$ 是否成立只取决于 $f_M$（输入输出行为），与 $M$ 的具体写法无关。等价地说，存在定义在部分函数集合上的性质 $\hat P$，使得 $\langle M\rangle \in P \iff f_M \in \hat P$。

###### 定义：非平凡性质

$P \subseteq \mathcal{L}$ 称为*非平凡的*，当且仅当 $P \ne \emptyset$ 且 $P \ne \mathcal{L}$。即：存在至少一台机器具有 $P$，也存在至少一台机器不具有 $P$。

###### 定义：可判定的

性质 $P \subseteq \mathcal{L}$ 称为*可判定的*，当且仅当存在一个图灵机 $R$（判定器），使得对任意 $\langle M \rangle \in \mathcal{L}$：

$$
R(\langle M \rangle) = \begin{cases}
\text{接受（输出"是"} & \text{若 } \langle M \rangle \in P \\
\text{拒绝（输出"否"} & \text{若 } \langle M \rangle \notin P
\end{cases}
$$

并且 $R$ 对任意输入都保证停机（$R$ 本身对应的函数是全函数）。也就是说，不仅要给出正确答案，还必须保证在有限时间内一定给出答案。

###### 定理：图灵停机定理（Halting Problem 的不可判定性）

性质 $HALT = \{\langle M, w \rangle : M \text{ 是图灵机},\ M \text{ 在输入 } w \text{ 上停机}\}$ 是不可判定的。

**证明（对角化法，Turing 1936）：**

反证。假设存在判定器 $H$，对任意 $\langle M, w\rangle$ 判定 $M(w)$ 是否停机。构造一台新机器 $D$：

$$D(\langle M \rangle) = \begin{cases} \text{死循环（不停机）} & \text{若 } H(\langle M, M\rangle) = \text{"停机"} \\ \text{停机（接受）} & \text{若 } H(\langle M, M\rangle) = \text{"不停机"} \end{cases}$$

即 $D$ 把 $M$ 的编码同时当作程序和输入喂给自身。考虑 $D(\langle D \rangle)$：

- 若 $D$ 在 $\langle D\rangle$ 上停机 $\Rightarrow$ 按 $D$ 的定义，条件是 $H(\langle D,D\rangle)=$"不停机" $\Rightarrow$ 矛盾；
- 若 $D$ 在 $\langle D\rangle$ 上不停机 $\Rightarrow$ 按 $D$ 的定义，条件是 $H(\langle D,D\rangle)=$"停机" $\Rightarrow$ 矛盾。

两种情况都自相矛盾，所以 $H$ 不存在。

###### 定理：Rice 定理

设 $P \subseteq \mathcal{L}$ 是**语义性质**且**非平凡**，则 $P$ **不可判定**。

**证明（归约到 HALT）：**

不失一般性设处处发散的函数 $\emptyset \notin \hat P$（否则对补集 $\bar P$ 证明，可判定性对补运算封闭）。由非平凡性，存在 $M_Y$ 使 $f_{M_Y} \in \hat P$。

假设存在判定器 $R$ 判定 $P$。给定任意 $HALT$ 实例 $\langle M, w\rangle$，构造机器 $M'$：

> $M'(x)$：先模拟 $M$ 在 $w$ 上运行；若这一步停机，则接着模拟 $M_Y(x)$ 并输出其结果；若不停机，$M'$ 也不停机。

- 若 $M$ 不在 $w$ 上停机 $\Rightarrow f_{M'} = \emptyset \notin \hat P \Rightarrow \langle M'\rangle \notin P$；
- 若 $M$ 在 $w$ 上停机 $\Rightarrow f_{M'} = f_{M_Y} \in \hat P \Rightarrow \langle M'\rangle \in P$。

$M'$ 可由 $\langle M, w\rangle$ 有效构造。于是 $M$ 在 $w$ 上停机 $\iff R(\langle M'\rangle)=$"是"，这就给出了 $HALT$ 的判定算法，与图灵停机定理矛盾。故 $R$ 不存在。∎

###### 为什么"数组越界"是语义性质

设 $P_{\text{OOB}} = \{\langle M \rangle : M \text{ 在其运行过程中会发生数组越界访问}\}$（把"是否越界"整理成 $M$ 运行行为的一部分，例如约定越界时输出特殊标记或崩溃状态）。

按语义性质的定义，要验证 $f_{M_1} = f_{M_2} \implies (\langle M_1\rangle \in P_{\text{OOB}} \iff \langle M_2\rangle \in P_{\text{OOB}})$。

**成立的原因：** "是否会越界"完全由程序在给定输入下的实际运行轨迹和读写行为决定——即完全由 $f_M$ 决定，不依赖代码是怎么写的。两份实现了同一输入输出行为（含是否越界）的代码，天然就在这条性质上取值一致。

它显然**非平凡**：存在会越界的程序（如 `a[n+1]` 访问越界），也存在不会越界的程序（如安全的边界检查代码）。因此 $P_{\text{OOB}}$ 是非平凡的语义性质，根据 Rice 定理，不存在通用算法能对所有程序精确判定其是否存在数组越界。这正是静态分析工具（包括 Testbed 中的 index range checker）只能通过 interval domain 抽象解释等方法做近似判断的原因——必须在"漏报"和"误报"之间取舍，无法两者兼得。

### 4.4 69D 中途初始化追踪的讨论

**目标：** 在"中途发现初始化语句"中，追踪一个变量的所有可能路径及路径上的初始化语句集合。这涉及数据流分支追踪，目前未实现完整版本（goto 等控制流更使问题复杂化）。当前实现只检查**同一语法层级**中位于引用语句之前的语句，作为保守近似。

**退行方案（同一层级数据流）：**

能否只看同一层级的数据流来判断误报？证明：这同一层级的初始化集是真实初始化集的子集。

对于线性编程而言，不考虑 goto 语句：

给定对变量 $x$ 的引用语句 $sta$：
- 设集合 $S_0$ 是当前函数块作用域中，从头执行到 $sta$ 处、且和 $sta$ 同一语法树层级的对变量 $x$ 执行初始化语句的集合；
- 设集合 $S$ 是当前函数块作用域中，从头执行到 $sta$ 处、对变量 $x$ 执行初始化语句的集合。由于经过不同分支，每次执行时的 $S$ 可能不同。

能否证明 $S_0 \subset S$？如果是函数式编程呢？

```c
void g(int);
void f(){
    int i;

    if (/* ?? */) {
        i = 1;
        return;
    }

    i = 1;
    g(i);
}
```

由于该问题未获得完备解，实现上采用"同一层级找到初始化/取地址证据才判误报，否则保守不判"的策略，保证不产生虚警。
